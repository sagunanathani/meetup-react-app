service: auth-server

provider:
  name: aws
  runtime: nodejs18.x
  region: eu-central-1
  deploymentBucket:
    name: meet-app-deploy-eu-central-1-023519301352-v2

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: hello
          method: get
